---
title: 'X-VLA代码略读-1'
publishDate: 'Mar 1, 2026'
updatedDate: 'Mar 1, 2026'
description: 'LeRobot代码库中的X-VLA完整实现'
tags:
  - Embodied AI
  - Paper Reading
language: 'Chinese'
heroImage: { src: './kizumono3.jpg', color: '#9698C1' }
---

import { GithubCard } from 'astro-pure/advanced'

<GithubCard repo='huggingface/lerobot' />

LeRobot是由Hugging Face开发的一个基于PyTorch的机器人学习框架，集成了整个机器人学习的技术栈，提供了一个良好的机器人学习平台。LeRobot参考[原始代码](https://github.com/2toinf/X-VLA)实现了X-VLA。本文根据huggingface/lerobot仓库中的代码快速浏览X-VLA的部分实现。

## 基础环境与文件结构

Ubunutu 24.04 + nvidia-driver-570 + prime-select on-demand + torch 2.10.0 cu128 + RTX5060环境可用。

在该配置下使用GPU，会碰到不兼容问题 `RuntimeError: CUDA error: CUBLAS_STATUS_INVALID_VALUE`。改一个PyTorch有关配置可解决：

```python
conda env config vars set -n lerobot TORCH_BLAS_PREFER_CUBLASLT=1
```

lerobot库中，有关X-VLA的核心代码集中在 `lerobot/src/lerobot/policies/xvla` 目录下：

```tree
.
├── action_hub.py				# 处理不同aaction space的格式和损失函数
├── configuration_florence2.py   # VLM config，包括模型层数、宽度、窗口长度等基本参数
├── configuration_xvla.py		# X-VLA配置，同上
├── __init__.py
├── modeling_florence2.py		# VLM神经网络框架，以及LeRobot训练/推理接口包装
├── modeling_xvla.py			    # X-VLA神经网络框架，以及LeRobot训练/推理接口包装
├── processor_xvla.py			# 对数据的预处理/后处理
├── soft_transformer.py			# 包含Soft Prompt输入的DiT实现
└── utils.py
```

下面主要介绍整体数据流和部分数据处理流程。

## 数据I/O形状

### Raw Data

```
=== Raw frame keys ===
dict_keys(['observation.images.image', 'observation.images.image2',
           'observation.state', 'action', 'timestamp', 'frame_index',
           'episode_index', 'index', 'task_index', 'task'])
action shape: torch.Size([7])
```

- `observation.images.image`: 主视角RGB，H * W * 3
- `observation.images.image2`: 其他视角RGB
- `observation.state`: *8维*本体状态
- `action`: *7维*动作演示$[Δx,Δy,Δz,Δr_1​,Δr_2​,Δr_3​,gripper]$
- `timestamp`, `frame_index`, `episode_index`, `index`: 一些时间步编号、帧序号，用来在一个 episode 里索引
- `task`: 任务

### Preprocess

```
=== Batch keys after preprocess ===
action <class 'torch.Tensor'> torch.Size([1, 7])
next.reward <class 'float'> None None
next.done <class 'bool'> None None
next.truncated <class 'bool'> None None
info <class 'dict'> None None
task <class 'list'> None None
index <class 'torch.Tensor'> torch.Size([1])
task_index <class 'torch.Tensor'> torch.Size([1])
episode_index <class 'torch.Tensor'> torch.Size([1])
domain_id <class 'torch.Tensor'> torch.Size([1])
observation.images.image  torch.Size([1, 3, 256, 256])
observation.images.image2 torch.Size([1, 3, 256, 256])
observation.state         torch.Size([1, 8])
observation.language.tokens          torch.Size([1, 50])
observation.language.attention_mask  torch.Size([1, 50])
```

- `next.reward <class 'float'> None None  ... task <class 'list'> None None`: 用于RL的奖励、终止标识等，LIBERO数据集里没有故为None
- 图像：`[H, W, 3] -> [1, 3, H, W]` 加入batch维，resize+padding，像素映射到 `[0, 1]`，使得Florence2视觉编码器能够处理
- `observation.state`: 8维状态

### Pred Action

```
=== pred action (model output) ===
type: <class 'torch.Tensor'>
shape: torch.Size([1, 20])
dtype: torch.float32

Single action vector (squeezed):
shape: torch.Size([20])
values (first few dims):
tensor([-0.0439, -0.1054, 0.2481, 0.1319, -0.2037, 0.1266, 0.0398, 0.0038,
        -0.4194, 0.4054, ...])
```

有多种动作格式(**Action Mode**): 

- `ee6d`: 20-dim action
- `auto`: 自动检测
- joint, agibot_ee6d, so101_bimanual等

Action Mode会决定：1. loss计算方式（不同维度用不同loss） 2. 预处理方式（如何将真实维度转为20维） 3. 后处理方式（如何将20维转为真实维度，如裁剪）

对于 `auto`：

- 从数据集中读取`action_feature.shape[-1]`，作为`real_dim`
- loss在前real_dim个维度计算
- 后处理裁减到 `real_dim` 维

## processor_xvla.py

对数据做预处理和后处理，用def_xvla_pre_post_processors函数封装，返回预处理和后处理pipeline
```python
def make_xvla_pre_post_processors(
	config: XVLAConfig,
	dataset_stats: dict[str, dict[str, torch.Tensor]] | None = None,
) -> tuple[
	PolicyProcessorPipeline[dict[str, Any], dict[str, Any]],
	PolicyProcessorPipeline[PolicyAction, PolicyAction],
]:
	"""
	Build the LeRobot processor pipelines for XVLA.
	"""

	features = {**config.input_features, **config.output_features}
	input_steps = [
		# 1) 字段名重命名（不同数据集字段可能不同）
		RenameObservationsProcessorStep(rename_map={}),
		# 2) 补 batch 维：单样本从 (C,H,W) -> (1,C,H,W)
		AddBatchDimensionProcessorStep(),
		# 3) 将文本任务指令 token 化，生成 input_ids / attention 等
		TokenizerProcessorStep(
			tokenizer_name=config.tokenizer_name,
			max_length=config.tokenizer_max_length,
			padding=config.pad_language_to,
			padding_side=config.tokenizer_padding_side,
		),
		# 4) 图像值域对齐到 [0, 1]
		XVLAImageToFloatProcessorStep(),
		# 5) 图像标准化到 ImageNet 统计分布
		XVLAImageNetNormalizeProcessorStep(),
		# 6) 加 domain_id（用于多 embodiment/多域提示）
		XVLAAddDomainIdProcessorStep(),
		# 7) 把张量放到目标设备（cpu/cuda）
		DeviceProcessorStep(device=config.device),
		# 8) 对 state/action 做归一化（若提供了 dataset_stats）
		NormalizerProcessorStep(
			features=features, norm_map=config.normalization_mapping, stats=dataset_stats
		),
	]
	output_steps = [
		# 推理输出逆归一化
		UnnormalizerProcessorStep(
			features=config.output_features,
			norm_map=config.normalization_mapping,
			stats=dataset_stats,
		),
		# 后处理后统一转到 CPU，方便 env.step(np) 或日志保存
		DeviceProcessorStep(device="cpu"),
	]
	
	return (
		PolicyProcessorPipeline[dict[str, Any], dict[str, Any]](
			steps=input_steps,
			name=POLICY_PREPROCESSOR_DEFAULT_NAME,
		),
		PolicyProcessorPipeline[PolicyAction, PolicyAction](
			steps=output_steps,
			name=POLICY_POSTPROCESSOR_DEFAULT_NAME,
			to_transition=policy_action_to_transition,
			to_output=transition_to_policy_action,
		),
	)

# 更细节实现代码省略

def make_xvla_libero_pre_post_processors() -> tuple[
    PolicyProcessorPipeline[dict[str, Any], dict[str, Any]],
    PolicyProcessorPipeline[PolicyAction, PolicyAction],
]:
    """
    Build the LeRobot processor pipelines for XVLA with LIBERO environment.
    """
    # 这个 pipeline 是“环境侧”处理，与上面的“通用训练/推理处理”互补。
    # - pre: 把 LIBERO 原始 observation 映射到 XVLA 所需字段
    # - post: 把模型动作从 6D 旋转表示转换回 env 更易用表示
    pre_processor_steps: list[ProcessorStep] = []
    post_processor_steps: list[ProcessorStep] = []
    # LiberoProcessorStep具体包括：图像信息旋转180度，proprio补充维度+拼接，动作（6D旋转角）拼接
    pre_processor_steps.extend(
        [LiberoProcessorStep(), XVLAImageNetNormalizeProcessorStep(), XVLAAddDomainIdProcessorStep()]
    )
    post_processor_steps.extend([XVLARotation6DToAxisAngleProcessorStep()])
    return (
        PolicyProcessorPipeline[dict[str, Any], dict[str, Any]](
            steps=pre_processor_steps,
        ),
        PolicyProcessorPipeline[PolicyAction, PolicyAction](
            steps=post_processor_steps,
        ),
    )
```

## modeling_xvla. py

分为两层：

-  `XVLAModel`: 纯神经网络本体框架，VLM 编码 + action DiT
-  `XVLAPolicy`: LeRobot 训练/推理接口包装，准备 batch、队列出动作等

###  XVLAModel
```python
class XVLAModel(nn.Module):
    """
    XVLA backbone that stitches Florence-2 embeddings with the temporal/action transformer head.
    """

    def __init__(
        self,
        config: XVLAConfig,
        florence_config: Florence2Config,
        proprio_dim: int,
    ) -> None:
		super().__init__()
		self.config = config
		self.chunk_size: int = config.chunk_size
		self.use_proprio: bool = config.use_proprio
		
		# 确定action space中动作维度数的代码，此处省略
		# "auto" 模式下自动匹配action space应有的维度
		
		self.dim_action = self.action_space.dim_action
		self.dim_proprio = proprio_dim
		
		# Florence2 是视觉+语言编码器
		self.vlm = Florence2ForConditionalGeneration(florence_config)
		if hasattr(self.vlm, "language_model"):
		    lm = self.vlm.language_model
		    if hasattr(lm, "model") and hasattr(lm.model, "decoder"):
		        # XVLA 只需要 encoder 特征，不做自回归文本解码
		        del lm.model.decoder
		    if hasattr(lm, "lm_head"):
		        del lm.lm_head
		
		projection_dim = getattr(self.vlm.config, "projection_dim", None)
		if projection_dim is None:
		    raise ValueError("Florence2 config must provide `projection_dim` for multimodal fusion.")
		
		# 包含soft prompt参数的transformer模块
		self.transformer = SoftPromptedTransformer(
            hidden_size=config.hidden_size,
            multi_modal_input_size=projection_dim,
            depth=config.depth,
            num_heads=config.num_heads,
            mlp_ratio=config.mlp_ratio,
            num_domains=config.num_domains,
            dim_action=self.dim_action,
            dim_propio=self.dim_proprio,
            len_soft_prompts=config.len_soft_prompts,
            dim_time=config.dim_time,
            max_len_seq=config.max_len_seq,
            use_hetero_proj=config.use_hetero_proj,
        )
		
		# 对dtype的对齐，
		# 以及训练时冻结哪些模块的有关代码，此处省略
		# 按照论文，预训练时冻结Florence视觉-语言编码器，训练soft prompt和policy transformer
	
	# vlm前向过程
	def forward_vlm(
        self,
        input_ids: torch.LongTensor,
        pixel_values: torch.FloatTensor,
        image_mask: torch.Tensor,
    ) -> dict[str, torch.Tensor]:
        """
        Encode text and multi-view images via Florence2 encoder.
        """
        # pixel_values 常见形状：(B, V, C, H, W)
        # B: batch size, V: camera views
        batch_size, num_views = pixel_values.shape[:2]
        flat_mask = image_mask.view(-1).to(dtype=torch.bool)
        # flatten(0,1): (B,V,C,H,W) -> (B*V,C,H,W)
        flat_images = pixel_values.flatten(0, 1)
        num_valid = int(flat_mask.sum().item())
        if num_valid == 0:
            raise ValueError("At least one image view must be valid per batch.")

        # 仅编码有效视角，避免对 padding 视角做无效计算
        valid_images = flat_images[flat_mask]
        valid_feats = self.vlm._encode_image(valid_images)
        tokens_per_view, hidden_dim = valid_feats.shape[1:]

        # 再散射回原始 (B*V, tokens, hidden) 布局，padding 位保持 0
        image_features = valid_feats.new_zeros((batch_size * num_views, tokens_per_view, hidden_dim))
        image_features[flat_mask] = valid_feats
        image_features = image_features.view(batch_size, num_views, tokens_per_view, hidden_dim)
        
        # 文本 token id -> token embedding
        inputs_embeds = self.vlm.get_input_embeddings()(input_ids)
        merged_embeds, attention_mask = self.vlm._merge_input_ids_with_image_features(
            image_features[:, 0],
            inputs_embeds,
        )

        enc_out = self.vlm.language_model.model.encoder(
            attention_mask=attention_mask,
            inputs_embeds=merged_embeds,
        )[0]

        # 剩余视角（第 2..V 个）作为辅助视觉 token，喂给后续 transformer
        aux_visual_inputs = image_features[:, 1:].reshape(batch_size, -1, hidden_dim)
        return {"vlm_features": enc_out, "aux_visual_inputs": aux_visual_inputs}
    
    # 总体前向过程
    def forward(
        self,
        input_ids: torch.LongTensor,
        image_input: torch.FloatTensor,
        image_mask: torch.Tensor,
        domain_id: torch.LongTensor,
        proprio: torch.Tensor,
        action: torch.Tensor,
    ) -> dict[str, torch.Tensor]:
        """
        Forward pass for the XVLA model.
        """
        target_dtype = self._get_target_dtype()
        # 训练时统一 dtype，减少混合精度下的隐式转换
        image_input = image_input.to(dtype=target_dtype)
        proprio = proprio.to(dtype=target_dtype)
        action = action.to(dtype=target_dtype)

		# 调用上面vlm的前向过程，编码数据集中的文本和图像
        enc = self.forward_vlm(input_ids, image_input, image_mask)

        batch_size = input_ids.shape[0]
        # Flow matching 中的时间步 t in [0,1)
        # 这里给每个 batch 样本不同 t，覆盖不同噪声强度
        t = (
            torch.rand(1, device=input_ids.device, dtype=target_dtype)
            + torch.arange(batch_size, device=input_ids.device, dtype=target_dtype) / batch_size
        ) % (1 - 1e-5)

        # x_t = t * noise + (1-t) * action
        action_noisy = torch.randn_like(action) * t.view(-1, 1, 1) + action * (1 - t).view(-1, 1, 1)
        proprio_m, action_noisy_m = self.action_space.preprocess(proprio, action_noisy)

        pred_action = self.transformer(
            domain_id=domain_id,
            action_with_noise=action_noisy_m,
            t=t,
            proprio=proprio_m,
            **enc,
        )
        # compute_loss 返回的是分项 loss 字典（例如 position_loss, gripper_loss）
        return self.action_space.compute_loss(pred_action, action)
    
    # 推理过程    
    @torch.no_grad()
    def generate_actions(
        self,
        input_ids: torch.LongTensor,
        image_input: torch.FloatTensor,
        image_mask: torch.Tensor,
        domain_id: torch.LongTensor,
        proprio: torch.Tensor,
        steps: int,
    ) -> torch.Tensor:
        self.eval()

        target_dtype = self._get_target_dtype()
        image_input = image_input.to(dtype=target_dtype)
        proprio = proprio.to(dtype=target_dtype)

        enc = self.forward_vlm(input_ids, image_input, image_mask)

        batch_size = input_ids.shape[0]
        action_dim = self.dim_action

        # x1: 纯高斯噪声（起点）
        x1 = torch.randn(batch_size, self.chunk_size, action_dim, device=proprio.device, dtype=target_dtype)
        # action: 估计的干净动作（初值全 0）
        action = torch.zeros_like(x1)

        # 反向迭代去噪：t = 1 -> 0
        steps = max(1, int(steps))
        for i in range(steps, 0, -1):
            t = torch.full((batch_size,), i / steps, device=proprio.device, dtype=target_dtype)
            x_t = x1 * t.view(-1, 1, 1) + action * (1 - t).view(-1, 1, 1)
            proprio_m, x_t_m = self.action_space.preprocess(proprio, x_t)
            action = self.transformer(
                domain_id=domain_id,
                action_with_noise=x_t_m,
                proprio=proprio_m,
                t=t,
                **enc,
            )
        # 某些 action_space 会在这里做后处理（例如夹爪 sigmoid）
        return self.action_space.postprocess(action)
```

### XVLAPolicy

```python
class XVLAPolicy(PreTrainedPolicy):
    """LeRobot-compliant wrapper built around the XVLA model."""

    config_class = XVLAConfig
    name = "xvla"

    def __init__(self, config: XVLAConfig, **kwargs):
        super().__init__(config)
        config.validate_features()
        florence_config = config.get_florence_config()
        # dim_proprio 是模型内部预留的 proprio 维度上限
        proprio_dim = config.max_state_dim if config.use_proprio else 0
        self.model = XVLAModel(config=config, florence_config=florence_config, proprio_dim=proprio_dim)
        self.reset()
    
    def reset(self) -> None:
        """
        动作队列：模型一次会产出 chunk_size 个未来动作，select_action 每次弹出 1 个执行
        """
        self._queues = {
            ACTION: deque(maxlen=self.config.n_action_steps),
        }
        
    def get_optim_params(self) -> dict:
        """
        检查所有参数的requires_grad属性，将所有参与梯度计算的参数转换为字典，并返回，代码略
        """
        return dict(filter(lambda kv: kv[1].requires_grad, self.named_parameters()))
    
    def _prepare_state(self, batch: dict[str, Tensor], batch_size: int, device: torch.device) -> Tensor:
    	"""
    	取最后一帧proprio，考虑缺失情况，补齐到指定形状，代码略
    	state: (B, D)
    	"""
    	return pad_vector(state, self.model.dim_proprio)
    
    def _prepare_images(self, batch: dict[str, Tensor]) -> tuple[Tensor, Tensor]:
    	"""
    	准备好图像输入，只取最后一帧，考虑缺失和多视角，代码略
    	stacked_imgs: (B, V, C, H, W); stacked_masks: (B, V)
    	"""
        return stacked_imgs, stacked_masks
        
    def _get_domain_id(self, batch: dict[str, Tensor], batch_size: int, device: torch.device) -> Tensor:
    	"""
    	获取该episode的domain id
    	candidate: (B)
    	"""
    	return candidate.to(dtype=torch.long)
    
    def _prepare_action_targets(self, batch: dict[str, Tensor]) -> Tensor:
    	"""
    	准备目标动作，将动作统一成时序动作格式，并做裁剪/补零到指定形状，代码略
    	actions: (B, T, D)
    	"""
        return actions
        
    def _build_model_inputs(self, batch: dict[str, Tensor]) -> dict[str, Tensor]:
        # 关键拼装点：把 batch 中分散字段整理成 model.forward 所需参数
        input_ids = batch[OBS_LANGUAGE_TOKENS]
        batch_size = input_ids.shape[0]
        images, image_mask = self._prepare_images(batch)
        domain_id = self._get_domain_id(batch, batch_size, images.device)
        proprio = self._prepare_state(batch, batch_size, images.device)
        return {
            "input_ids": input_ids,
            "image_input": images,
            "image_mask": image_mask,
            "domain_id": domain_id,
            "proprio": proprio,
        }
    
    def forward(self, batch: dict[str, Tensor]) -> tuple[Tensor, dict]:
        inputs = self._build_model_inputs(batch)
        targets = self._prepare_action_targets(batch)
        losses = self.model(action=targets, **inputs)
        # sum(dict_values) -> 标量总 loss，供反向传播
        total_loss = sum(losses.values())

        # .detach().item():
        # - detach: 从计算图中分离，避免日志记录持有图
        # - item: 单元素 tensor -> Python float
        log_dict = {k: v.detach().item() for k, v in losses.items()}
        log_dict["loss"] = total_loss.detach().item()
        return total_loss, log_dict
        
    def _get_action_chunk(self, batch: dict[str, Tensor]) -> Tensor:
        inputs = self._build_model_inputs(batch)
        # 一次生成一段动作序列 (B, chunk_size, action_dim)
        actions = self.model.generate_actions(**inputs, steps=self.config.num_denoising_steps)
        return actions

    @torch.no_grad()
    def predict_action_chunk(self, batch: dict[str, Tensor], noise: Tensor | None = None) -> Tensor:  # noqa: ARG002
        self.eval()
        self._queues = populate_queues(self._queues, batch, exclude_keys=[ACTION])
        return self._get_action_chunk(batch)

    @torch.no_grad()
    def select_action(self, batch: dict[str, Tensor], noise: Tensor | None = None) -> Tensor:  # noqa: ARG002
        self.eval()
        self._queues = populate_queues(self._queues, batch, exclude_keys=[ACTION])

        if len(self._queues[ACTION]) == 0:
            actions = self._get_action_chunk(batch)
            # transpose(0,1): (B,T,D) -> (T,B,D)，方便按时间逐条出队
            self._queues[ACTION].extend(actions.transpose(0, 1)[: self.config.n_action_steps])

        # 每次环境 step 只返回一个动作
        return self._queues[ACTION].popleft()
        
    @classmethod
    def from_pretrained(
        cls: builtins.type[T],
        pretrained_name_or_path: str | Path,
        *,
        config: PreTrainedConfig | None = None,
        force_download: bool = False,
        resume_download: bool | None = None,
        proxies: dict | None = None,
        token: str | bool | None = None,
        cache_dir: str | Path | None = None,
        local_files_only: bool = False,
        revision: str | None = None,
        strict: bool = False,
        **kwargs,
    ):
        """
        加载预训练的XVLA权重，忽略应该随机初始化的层，代码略
        """
        return instance
```

## action_hub.py

action hub确定了动作的表示格式和action token loss计算方式。该文件将上述差异统一封装到BaseActionSpace的子类中。

三种典型的action space：

1. "ee6d"(End-effector + 6D rotation): 双臂末端XYZ绝对位置（3 * 2）+6D旋转（6 * 2）+夹爪（1 * 2）共20维，在夹爪上计算BCE，在其他维度上计算MSE
2. "auto": 传入真实维度数，在真实维度上计算MSE
3. "so101_bimanual": 双臂各有5个关节+1个夹爪，共12维，均计算MSE

### ee6d

```python
@register_action("ee6d")
class EE6DActionSpace(BaseActionSpace):
    """End-effector layout with xyz, 6D rotation, and gripper channels."""
    # 双臂末端位置 + 6D 旋转 + 夹爪 = 20 维
    dim_action = 20
    gripper_idx = (9, 19)
    GRIPPER_SCALE = 1.0
    XYZ_SCALE = 500.0
    ROT_SCALE = 10.0

    POS_IDX_1 = (0, 1, 2)
    POS_IDX_2 = (10, 11, 12)
    ROT_IDX_1 = (3, 4, 5, 6, 7, 8)
    ROT_IDX_2 = (13, 14, 15, 16, 17, 18)

    def __init__(self):
        super().__init__()
        # MSE 用于连续变量（位置、旋转）
        self.mse = nn.MSELoss()
        # BCEWithLogitsLoss = sigmoid + BCE（数值更稳定）
        # 因此训练时 gripper 通道应保持“logits”而不是先 sigmoid
        self.bce = nn.BCEWithLogitsLoss()

    def compute_loss(self, pred, target):
        assert pred.shape == target.shape, "pred/target shapes must match"
        # 这里解包维度主要用于可读性和 sanity check
        batch_size, seq_len, action_dim = pred.shape
        _ensure_indices_valid(action_dim, self.gripper_idx, "gripper_idx")

        # Gripper BCE
        # 每个夹爪通道单独算 loss，再取平均
        g_losses = [self.bce(pred[:, :, gi], target[:, :, gi]) for gi in self.gripper_idx]
        gripper_loss = sum(g_losses) / len(self.gripper_idx) * self.GRIPPER_SCALE

        # XYZ position
        # 双臂位姿：左/右末端位置分别求 MSE
        pos_loss = (
            self.mse(pred[:, :, self.POS_IDX_1], target[:, :, self.POS_IDX_1])
            + self.mse(pred[:, :, self.POS_IDX_2], target[:, :, self.POS_IDX_2])
        ) * self.XYZ_SCALE

        # Rotation 6D
        rot_loss = (
            self.mse(pred[:, :, self.ROT_IDX_1], target[:, :, self.ROT_IDX_1])
            + self.mse(pred[:, :, self.ROT_IDX_2], target[:, :, self.ROT_IDX_2])
        ) * self.ROT_SCALE

        return {
            "position_loss": pos_loss,
            "rotate6D_loss": rot_loss,
            "gripper_loss": gripper_loss,
        }

    def preprocess(self, proprio, action, mode="train"):
        """Zero-out gripper channels in proprio/action."""
        proprio_m = proprio.clone()
        action_m = action.clone()
        # 将夹爪通道置 0，常用于减少不稳定或遵循原始实现约定
        proprio_m[..., self.gripper_idx] = 0.0
        action_m[..., self.gripper_idx] = 0.0
        return proprio_m, action_m

    def postprocess(self, action: torch.Tensor) -> torch.Tensor:
        """Apply sigmoid to gripper logits."""
        if action.size(-1) > max(self.gripper_idx):
            action[..., self.gripper_idx] = torch.sigmoid(action[..., self.gripper_idx])
        return action
```

### auto

和ee6d相比，主要多了补齐维度的操作。

```python
@register_action("auto")
class AutoActionSpace(BaseActionSpace):
    """
    Auto-detecting action space that adapts to any action dimension.

    - Auto-detects the real action dimension from the policy feature
    - Model outputs max_dim for compatibility with pretrained models
    - Loss is computed only on the first real_dim dimensions
    - Postprocess trims output back to real_dim

    Args:
        real_dim: The actual action dimension from the dataset/policy feature
        max_dim: The model's output dimension for pretrained VLA compatibility
    """

    JOINTS_SCALE = 1.0

    def __init__(self, real_dim: int, max_dim: int):
        super().__init__()
        # real_dim: 当前数据集真实动作维度
        # dim_action: 模型内部统一维度（通常与预训练对齐）
        self.real_dim = real_dim
        self.dim_action = max_dim  # Model-facing dimension
        self.mse = nn.MSELoss()

    def _pad_to_model_dim(self, x: torch.Tensor) -> torch.Tensor:
        """Pad real_dim → max_dim (zeros for the dummy channels)."""
        if x is None:
            return None
        if x.size(-1) == self.dim_action:
            return x
        if x.size(-1) != self.real_dim:
            # If dimension doesn't match either, pad/trim to real_dim first
            if x.size(-1) < self.real_dim:
                pad_shape = list(x.shape[:-1]) + [self.real_dim - x.size(-1)]
                pad = x.new_zeros(pad_shape)
                x = torch.cat([x, pad], dim=-1)
            else:
                x = x[..., : self.real_dim]

        pad_shape = list(x.shape[:-1]) + [self.dim_action - self.real_dim]
        pad = x.new_zeros(pad_shape)
        return torch.cat([x, pad], dim=-1)

    def _trim_to_real_dim(self, x: torch.Tensor) -> torch.Tensor:
        """Trim model output max_dim → real_dim."""
        return x[..., : self.real_dim]

    def compute_loss(self, pred: torch.Tensor, target: torch.Tensor) -> dict[str, torch.Tensor]:
        """
        Compute loss only on the first real_dim dimensions.

        pred:   [B, T, max_dim] from the model
        target: [B, T, real_dim] or [B, T, max_dim]

        Loss = MSE(pred[:,:,:real_dim], target[:,:,:real_dim])
        """
        pred = self._pad_to_model_dim(pred)
        target = self._pad_to_model_dim(target)
        assert pred.shape == target.shape, f"Shape mismatch: pred {pred.shape} vs target {target.shape}"

        # only compute loss on the real dimensions
        # 这样 dummy/padding 维不会干扰训练信号
        joints_loss = (
            self.mse(
                pred[:, :, : self.real_dim],
                target[:, :, : self.real_dim],
            )
            * self.JOINTS_SCALE
        )

        return {"joints_loss": joints_loss}

    def preprocess(self, proprio: torch.Tensor, action: torch.Tensor, mode: str = "train"):
        """
        Pad action from real_dim to max_dim for the model.
        """
        return proprio, self._pad_to_model_dim(action)

    def postprocess(self, action: torch.Tensor) -> torch.Tensor:
        """
        Trim model output from max_dim to real_dim for real robot control.
        """
        return self._trim_to_real_dim(action)
```